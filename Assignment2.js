// Create a program that prompts the user to enter a number and then checks whether that number is even or odd.
// Display the result to the user using an alert box.
let num = prompt("Enter your Number:");
if(num%2==0){
    alert(`${num} is an even number.`)
}
else{
    alert(`${num} is an odd number.`)
}
